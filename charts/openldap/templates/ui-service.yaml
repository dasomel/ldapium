{{- if .Values.ui.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "openldap.ui.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openldap.ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - name: http
      port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "openldap.ui.selectorLabels" . | nindent 4 }}
{{- end }}
